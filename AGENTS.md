# AGENTS

Start from the docs index:

```text
docs/index.md
```

Do not duplicate architecture rules here.

Do not duplicate styling rules here.

Update `docs/index.md` when a new docs file is added.

Update `docs/architecture.md` when architecture changes.

Update `docs/styling.md` when styling rules change.

Use strict TypeScript.

Do not use `any`.

Keep Electron IPC typed.

Run Nx commands from the repository root.